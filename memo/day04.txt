switch

1. 조건문
switch(변수명){
	case 값1:

변수에 정수형( int,long),문자형


2)while문
	while(조건식){
		실행할 문장;
	}

	조건식이 참일 동안 반복한다
	
실행중 조건식이 거짓이 될 수 있어야 멈춘다

4)do~while문
	while문과 동일하지만 최소 한 번은 무조건 실행해야할 때 사용한다

	do{
		실핼할문장

5)이중for문 
	for(초기식;조건식;증감식){
		for(초기식;조건식;증감식){
		}
	}
4.배열
	문자열 => 문자들의 나열
	배열 => 저장공간의 나열

배열의 사용이유
	변수를 여러개 선언하면 이름도 여러개 생긴다 ->각 저장공간을 관리하기 불편하다
	n칸 배열을 선언하면 저장공간 여러개 생기고 이름은 1개만 생긴다 -> 관리하기 편하다
	2.규칙성이 없는 값에 규칙성을 부여하기 위해서 (index)
	
변수 : [] 하나의 타입으로 된 하나의 값만 저장할수있는 저장공간
배열 : [][][]하나의 타입으로 된 여러개의 값을 저장할수있는 저장공간

배열의 선언
1) 배열의 변수 선언 후 배열 생성
	자료형 변수명 = 값;
	자료형 배열명 = new 자료형[배열크기];
	//어떤값을 넣을 지 모르나 몇칸 만들지는 알때 
	
	int[] num = new int [3];
	
	
2) 배열 변수 선언과 함께 초기값 할당
	자료형[] 배열명 = {값1,값2,값3,...};
	//어떤값을 넣을 지 알때 사용
	
3) 배열 변수 선언과 배열생성과 초기화
	자료형[] 배열명;
	배열묭 = new자료형[]{값1,값2,값3,...};

4) 배열을 생성하지 않고, 배열 변수를 초기화 하는 방법
	배열명을 선언하게 되면 배열의 크기나 값을 정하지 않은 상태로 현재는 사용하지 않으나
	이후에 값을 
	